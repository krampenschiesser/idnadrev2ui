import { DelegationPipe } from './delegation.pipe';

describe('DelegationPipe', () => {
  it('create an instance', () => {
    const pipe = new DelegationPipe();
    expect(pipe).toBeTruthy();
  });
});
